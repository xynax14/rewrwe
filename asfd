<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Для тебя 💜</title>
    <style>
        /* Фон с градиентом */
        body {
            background: linear-gradient(45deg, #8a2be2, #d8a7e3, #c89be6);
            background-size: 300% 300%;
            animation: backgroundAnimation 8s infinite alternate;
            font-family: "Arial", sans-serif;
            text-align: center;
            margin: 0;
            padding: 0;
            overflow: hidden;
        }
        @keyframes backgroundAnimation {
            0% { background-position: 0% 50%; }
            100% { background-position: 100% 50%; }
        }

        /* Заголовки */
        h1 {
            color: white;
            font-size: 3em;
            text-shadow: 2px 2px 10px rgba(255, 255, 255, 0.8);
            animation: fadeIn 2s ease-in-out infinite alternate;
        }
        @keyframes fadeIn {
            0% { opacity: 1; }
            100% { opacity: 0.8; }
        }

        /* Кнопки */
        button {
            padding: 15px 30px;
            font-size: 1.2em;
            background: white;
            border: none;
            border-radius: 10px;
            cursor: pointer;
            margin: 10px;
            box-shadow: 2px 2px 10px rgba(255, 255, 255, 0.5);
            transition: 0.3s;
        }
        button:hover {
            background: #e5b3ff;
            transform: scale(1.1);
        }

        /* Лепестки роз */
        .petal {
            position: absolute;
            width: 15px;
            height: 15px;
            background: pink;
            border-radius: 50%;
            opacity: 0.8;
            animation: fall 5s linear infinite;
        }
        @keyframes fall {
            0% { transform: translateY(0px) rotate(0deg); opacity: 1; }
            100% { transform: translateY(100vh) rotate(360deg); opacity: 0; }
        }

        /* Секретное сообщение */
        .secret-message {
            display: none;
            background: white;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 2px 2px 10px rgba(255, 255, 255, 0.5);
            margin-top: 20px;
        }

        /* Обратный отсчет */
        .countdown {
            font-size: 1.5em;
            color: white;
            margin-top: 20px;
        }

        /* 3D карточка */
        .flip-card {
            perspective: 1000px;
            display: inline-block;
            margin-top: 20px;
        }
        .flip-card-inner {
            width: 200px;
            height: 100px;
            transform-style: preserve-3d;
            transition: transform 0.8s;
        }
        .flip-card:hover .flip-card-inner {
            transform: rotateY(180deg);
        }
        .flip-card-front, .flip-card-back {
            position: absolute;
            width: 100%;
            height: 100%;
            backface-visibility: hidden;
            display: flex;
            align-items: center;
            justify-content: center;
            background: white;
            border-radius: 10px;
        }
        .flip-card-back {
            transform: rotateY(180deg);
            background: #e5b3ff;
        }
    </style>
</head>
<body>
    <h1>Ты у меня самая лучшая!</h1>

    <button onclick="showSurprise()">✨ Показать сюрприз</button>
    <button onclick="toggleMessage()">💌 Секретное сообщение</button>

    <!-- Сюрприз-открытка -->
    <div class="secret-message" id="secretMessage">Ты делаешь этот мир прекраснее 💜</div>

    <!-- Обратный отсчет -->
    <div class="countdown" id="countdown">До нашей годовщины осталось: </div>

    <!-- 3D карточка -->
    <div class="flip-card">
        <div class="flip-card-inner">
            <div class="flip-card-front">
                💖 Нажми, чтобы увидеть послание
            </div>
            <div class="flip-card-back">
                Я тебя люблю! 💜
            </div>
        </div>
    </div>
<!-- Музыка -->
    <audio id="backgroundMusic" loop>
        <source src="https://www.bensound.com/bensound-music/bensound-love.mp3" type="audio/mp3">
    </audio>

    <script>
        function showSurprise() {
            // Запуск фейерверка (конфетти)
            for (let i = 0; i < 20; i++) {
                let petal = document.createElement("div");
                petal.classList.add("petal");
                petal.style.left = Math.random() * 100 + "vw";
                document.body.appendChild(petal);
                setTimeout(() => { petal.remove(); }, 5000);
            }
            alert("Ты мое счастье! 💜");
        }

        function toggleMessage() {
            let message = document.getElementById("secretMessage");
            if (message.style.display === "none") {
                message.style.display = "block";
            } else {
                message.style.display = "none";
            }
        }

        // Обратный отсчет
        function updateCountdown() {
            let eventDate = new Date("2025-12-31").getTime(); // Укажи свою дату
            let now = new Date().getTime();
            let timeLeft = eventDate - now;

            let days = Math.floor(timeLeft / (1000 * 60 * 60 * 24));
            let hours = Math.floor((timeLeft % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
            let minutes = Math.floor((timeLeft % (1000 * 60 * 60)) / (1000 * 60));
            let seconds = Math.floor((timeLeft % (1000 * 60)) / 1000);

            document.getElementById("countdown").innerText = До нашей годовщины осталось: ${days} дней, ${hours} часов, ${minutes} минут, ${seconds} секунд;
        }
        setInterval(updateCountdown, 1000);
    </script>
</body>
</html>
